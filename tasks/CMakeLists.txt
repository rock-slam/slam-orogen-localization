# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

find_package(LAPACK REQUIRED)
if (NOT LAPACK_FOUND)
    message (FATAL_ERROR "LAPACK not found!")
    return()
endif ()

include(localizationTaskLib)
ADD_LIBRARY(${LOCALIZATION_TASKLIB_NAME} SHARED 
    ${LOCALIZATION_TASKLIB_SOURCES})
add_dependencies(${LOCALIZATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${LOCALIZATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${LOCALIZATION_TASKLIB_DEPENDENT_LIBRARIES}
    ${LAPACK_LIBRARIES})
SET_TARGET_PROPERTIES(${LOCALIZATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LOCALIZATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LOCALIZATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LOCALIZATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/localization)

